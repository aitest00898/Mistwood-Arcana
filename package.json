{
  "name": "mistwood-arcana",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "tsc -b && vite build",
    "check": "npm run typecheck && npm run build",
    "prepare:hero16": "node scripts/prepare-hero-16.mjs",
    "prepare:selection-art": "node scripts/prepare-selection-art.mjs",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "typescript": "^5.6.3",
    "vite": "^6.0.5"
  }
}
